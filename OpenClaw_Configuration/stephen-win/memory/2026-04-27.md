# 2026-04-27

## A2A 三端连通修复与校准

- 已完成 Stephen / Zin / Trey 三端 A2A 连通核验。
- 最终状态：
  - Stephen：已验通，返回 `STEPHEN_A2A_OK`
  - Zin：已验通，返回 `ZIN_A2A_OK`
  - Trey：已验通，直连返回 `A2A_OK`，封装返回 `WRAPPER_OK`

## 关键修正

- Stephen 之前记录的 token `27afdbc1` 不完整，导致误判。
- 本机 `a2a-gateway` 配置中实际生效的 Stephen token 为：
  - `27afdbc16350894e`
- Stephen 对外 A2A 地址最终确认：
  - `http://100.80.230.42:18800`
- Stephen 的 `agentCard.url` 当前配置为：
  - `http://100.80.230.42:18800/a2a/jsonrpc`

## 已落地文件

- `K:\openclaw-workspace\Scripts\trey-env.ps1`
  - 新增 Trey 本地 token 装载脚本
- `K:\openclaw-workspace\Scripts\trey-send.ps1`
  - 已改为优先加载 `trey-env.ps1`
- `K:\openclaw-workspace\Scripts\a2a-peers.local.json`
  - 已校正三端 peers：Stephen / Zin / Trey

## 经验结论

- A2A 排障时，先分清：
  1. gateway 端口
  2. A2A 插件端口
  3. agent card 路径
  4. 实际生效 token
- Stephen 这次问题的根因不是服务挂掉，也不是身份混淆，而是：
  - 记录下来的 token 被截短
- `a2a-send.mjs` 对 Stephen 使用 `http://100.80.230.42:18800` + 正确 token 后可正常完成闭环。

