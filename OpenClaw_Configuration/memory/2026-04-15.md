# 2026-04-15

- 已完成 Stephen 侧系统治理基线收口，目标是减少会话污染、减少 Telegram 审批打断、提升配置修改后的稳定感。
- 已记录新的协作身份口径：特雷（Trey / 特雷 / Hermes Agent）是运行在 101 服务器上的独立 Hermes Agent，由历史 OpenClaw / Zin 配置迁移后独立维护；后续协作中不得再将其混同为 OpenClaw 本体、辛，或旧实例别名。
- 已处理 QQBot 状态噪音问题：
  - 通过 `config.patch` 移除了 `plugins.entries.openclaw-qqbot` 与 `plugins.installs.openclaw-qqbot` 残留配置
  - 已删除残留目录：`C:\Users\a2832\.openclaw\extensions\qqbot`
  - 当前扩展目录已不再包含 `qqbot`
  - 结论：QQBot 已从“禁用但留残影”收敛为“配置与目录均清理完成”，后续状态命令的 QQBot 噪音源已被移除
- 已通过 `config.patch` 直接修改并生效：
  - `session.scope = per-sender`
  - `session.dmScope = per-peer`
  - `agents.defaults.heartbeat.target = none`
  - `agents.defaults.heartbeat.directPolicy = block`
  - `agents.defaults.heartbeat.isolatedSession = true`
  - `agents.defaults.heartbeat.lightContext = true`
  - `channels.telegram.execApprovals.enabled = false`
- 保留未动但已确认的基线：
  - `tools.exec.security = full`
  - `tools.exec.ask = off`
  - `channels.telegram.heartbeat.showOk = false`
  - `channels.telegram.heartbeat.showAlerts = false`
- 已完成本轮验收：再次读取当前配置，确认以上修改已进入运行配置。
- 已新增 SOP 文档：`K:\openclaw-workspace\notes\resources\config-change-sop.md`
  - 固定顺序为：schema/check current -> minimal patch -> reload/restart -> probe -> 落盘
- 本轮未改的项：
  - 未收紧 `channels.telegram.groupPolicy = open`，因为涉及群聊可达性与使用习惯，影响面不够小，不在本轮低风险优化范围内。
  - 未继续扩大 heartbeat 相关配置项，因当前 schema 仅确认了默认 heartbeat 隔离字段，已先完成最关键收口。
- 结论：Stephen 侧现已与 Zin 近期验证过的非 A2A 治理经验基本对齐，尤其是 heartbeat 隔离、DM 会话隔离、Telegram exec 审批关闭三项已落地。
- 新增修正：已排查本轮 `exec denied: allowlist miss` 的真实根因，不是运行配置问题；当前运行配置已确认仍为：
  - `tools.exec.security = full`
  - `tools.exec.ask = off`
  - `channels.telegram.execApprovals.enabled = false`
- 结论：`allowlist miss` 来自单次 `exec` 调用里手动显式传了 `security=allowlist` 的旧习惯，而不是系统默认策略回退。
- 已更新两份本地规则文档，明确禁止在默认策略已满足时继续手动传 `security: "allowlist"`：
  - `K:\openclaw-workspace\notes\resources\config-change-sop.md`
  - `K:\openclaw-workspace\notes\resources\execution-preflight-checklist.md`
- 已做命令验证：
  - `python -c "import os; print(len(os.listdir(r'K:\openclaw-workspace'))); print('PROBE_OK')"` 成功返回 `22` 与 `PROBE_OK`
  - `openclaw status` 本次不再出现 `allowlist miss`，但被当前 QQBot 插件输出与进程终止噪音干扰，说明后续若继续要把状态探针做稳定，还需单独处理 QQBot 噪音问题。

## 新上下文同步（仅整理，不追旧单）
- 已纳入主题：
  1. Telegram 私聊污染归类为配置治理问题；关键控制面收敛为 `session.scope/session.dmScope` 与 heartbeat 的 `block/none/isolated/light` 组合。
  2. 高权限操作的低打断策略已明确：`tools.exec.security = full` + `tools.exec.ask = off` 有效；问题重点转为模板与执行习惯升级，禁止再手动传 `allowlist`。
  3. 系统级操作统一 SOP 固定为：`check -> minimal patch -> restart/reload -> probe -> 落盘`。
  4. `104 / Freemail / MoeMail / SQLite 锁` 视为同一条业务链路，需要按同链路经验维护口径而非拆散记。
  5. A2A 长期判断口径固定为：三层验收、`contextId` 持久化、先区分网络波动还是 A2A 真挂。
  6. 执行纪律进入机制化约束：必须有真实工具调用，禁止空播报，经验优先沉淀进 `skill/reference/memory/checklist`。
- 当前仍缺的补位点：
  - `104 / Freemail / MoeMail / SQLite 锁` 链路的统一专题沉淀还不够集中，后续应补成单一可搜索主题笔记。
  - A2A 的“三层验收”与“网络波动 vs A2A 真挂”判别标准，后续宜补成更明确的 checklist / reference。
  - 执行纪律机制化目前已形成规则，但还可继续固化到更多模板、SOP、预检清单中，减少再次回退到旧习惯。
- 适合继续和 Zin 定期同步的主题：
  - Telegram / heartbeat / DM 隔离类配置治理经验。
  - 高权限执行口径与模板升级，尤其是去 `allowlist` 旧习惯。
  - 系统级 SOP 与探针验证方式。
  - `104 / Freemail / MoeMail / SQLite 锁` 链路的最新实战结论。
  - A2A 稳定性验收、`contextId` 持久化与故障分层判断。
  - 执行纪律、落盘纪律与 skill/reference/checklist 的机制化沉淀。
- 新增修正：已将 `tools.exec.host` 从 `gateway` 改为 `auto`，并执行 gateway 重启以加载新配置。
- 当前结论：
  - `exec denied: allowlist miss` 已实测消失，普通只读 `exec` 在不显式传 `security` 时已恢复正常。
  - `host: "sandbox"` 的显式覆盖在当前工具链仍返回旧的 `configured host is gateway`，表现与已生效运行配置 (`tools.exec.host = auto`) 不一致，判断为 exec 路由层/会话侧缓存未同步，不是配置文件值未改。
  - 后续执行口径：默认不要再手动传 `security: "allowlist"`；若无必须，也不要显式钉死 `host: "sandbox"`，先走默认 `host=auto` / 不传 host 的路径。
- 补充修正：在 gateway 重启后的新消息里再次实测 `host:"sandbox"`，错误已明确收敛为 `exec host=sandbox requires a sandbox runtime for this session`。
  - 结论更新：问题根因不是旧缓存，而是当前主会话未启用 sandbox runtime。
  - 已按用户边界确认：**不启用 sandbox 模式**。
  - 已进一步把两份复用规则文档补成可直接照抄的 gateway/auto 兼容写法，明确：默认不传 `host`、不传 `security`，非必要不要写 `host:"sandbox"` + `security:"allowlist"` 组合。

