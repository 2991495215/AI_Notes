## 2026-03-07 任务总结 - 个人主页部署

- **目标**：将 `K:\Downloads\个人主页2.zip` 部署到 RackNerd VPS (`104.168.24.38`)。
- **关键步骤**：
    1. **环境准备**：确认 VPS 为 Debian 12，安装并配置了 `nginx` 和 `php8.2-fpm`。
    2. **端口解决**：发现端口 80 被占用，最终决定将主页部署至 **8081** 端口以避免冲突。
    3. **高效传输**：使用 `scp` 直接上传压缩包。
    4. **解压与权限**：将文件解压至 `/var/www/personal-page`，权限移交给 `www-data`。
    5. **Nginx 配置**：编写专用配置，实现安全限制并支持 PHP。
- **结果**：
    - 个人主页：`http://104.168.24.38:8081` 成功上线。
    - 原有服务：`http://104.168.24.38:80` 已成功恢复。
- **教训**：在 Windows 下向文件追加中文内容需注意编码，处理大文件优先选择 `scp`。

## 2026-03-07 任务总结 - VPS 环境优化与 1Panel 维护

- **1Panel 排错**：在尝试安装 OpenResty 时，1Panel 因内存不足报 `segmentation fault`。
- **端口清理**：查明 `sing-box` 占用了 80 端口，已将其停止并禁用自启，释放了标准 HTTP/HTTPS 端口。
- **虚拟内存扩容**：为解决安装时的内存瓶颈，新建了 2GB 的 Swap 文件 (`/swapfile_st`) 并实现持久化。目前总 Swap 提升至 3GB。
- **OpenResty 预配置**：大哥哥计划将 OpenResty 设为 HTTP:8088 / HTTPS:4433 (Host 模式)，已记录在 `MEMORY.md`。

