# 2026-04-29 — Daily Log

时间线（UTC+8）
- 

要点
- 

产物
- 

备注
- 
# 2026-04-29 — Daily Log

时间线（UTC+8）
- 

要点
- 用户要求将 Xboard 部署到 `23.19.228.7`，复用现有 `1Panel` / `openresty` 站点 `vpn.thirty30.top`。
- 已在 `23.19.228.7` 完成 Xboard 基础部署，站点 `https://vpn.thirty30.top` 可访问；管理入口为 `/144b73d9`。
- Xboard 管理员登录账号最终改为 `a2832893628@qq.com`，密码按用户要求重置为 `TYGVTYGV`。
- `vpn.thirty30.top` 现已从旧的 YAML relay 后端切换到 Xboard；OpenResty 反代目标改为 `127.0.0.1:7001`。
- 部署过程中曾遇到 Redis/数据库迁移问题，后续已修通到前台可打开状态。
- 用户后续开始在 Xboard 中配置节点，目标协议选择 `Hysteria2`。
- 用户明确说明：`104` 那台服务器里，`5418` 是密码，不是端口；可据此 SSH 到 `104.168.24.38`。
- 已核对 `104.168.24.38` 上 Hysteria2 服务端实际配置：监听 `7777/udp`，认证密码 `5a06070c`，证书位于 `/etc/hysteria/cert.crt`，证书主题 `CN = www.bing.com`，伪装站点为 `https://www.bing.com`。
- 已核对 `23` 上 Xboard 节点记录存在 `RN-hy2`，类型为 Hysteria，地址 `104.168.24.38`，端口 `7777`，但 `show=0`（不显示）。
- 用户当前诉求：让我同时检查 `23` 和 `104` 两台服务器，协助把 Hysteria2 节点配置调通。

产物
- 关键路径：`/opt/1panel/www/sites/vpn.thirty30.top/index/xboard`
- 关键站点：`https://vpn.thirty30.top`
- 管理入口：`https://vpn.thirty30.top/144b73d9`
- Hysteria2 服务端关键信息：`104.168.24.38:7777/udp`，密码 `5a06070c`，SNI/证书名 `www.bing.com`

备注
- 用户偏好：中文、简洁、直接，不喜欢长篇大论。
- 自动巡检类 EvoMap cron 若无实质变化，应静默，不要频繁打扰用户。
- 破坏性操作、不可逆操作、大额消耗前仍需确认；但低风险小额 credits 可按长期授权自动执行。

