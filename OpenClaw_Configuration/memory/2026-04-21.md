# 2026-04-21

## Freemail 修复与生产收口
- 已完成 Freemail 线上修复与验证，站点：`https://freemail.trent30.org`
- 已修复/确认恢复：
  - 用户管理分页协议对齐（前端 `page/size` 与后端兼容）
  - 用户名修改后端接通
  - 管理页前端错误处理补齐（`403/500` 不再假成功）
  - `strictAdmin` 才可进入用户管理页
  - 生产 D1 绑定纠正为：`6afcd234-449d-4086-b6d1-991d74a33ed0`
  - custom domain 路由恢复：`freemail.trent30.org`
  - `MAIL_DOMAIN` 恢复为完整 18 个域名
  - 真实外部邮件收件恢复，已确认真实来信可入库并显示
- 管理员密码已收口：
  - 生产环境变量 `ADMIN_PASSWORD` 已改为 `2B33CC6666`
  - 数据库中 `admin` 用户密码已同步为 `2B33CC6666`
  - 旧密码 `TYGVTYGV` 已验证失效
- 已新增仓库内收口文件：
  - `docs/production-deploy-and-regression.md`（生产部署与回归检查）
  - `wrangler.local.example.toml`（本地/演示配置示例）
  - `scripts/regression-check.ps1`（一键回归检查脚本）
- 已跑通一键回归脚本：
  - 管理员登录通过
  - 旧密码拒绝通过
  - 域名数量检查通过（18）
  - 生成邮箱通过
  - 用户分页接口通过
  - `/receive` 注入测试通过，邮件可写入并读出
- 教训：生产配置、演示配置、临时调试配置必须分开；部署前必须先跑 `wrangler deploy --dry-run` 核对 D1 / route / vars。

