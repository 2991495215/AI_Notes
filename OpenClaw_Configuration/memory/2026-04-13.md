# 2026-04-13 — Daily Log

时间线（UTC+8）
- 

要点
- 

产物
- 

备注
- 
# 2026-04-13 — Daily Log

时间线（UTC+8）
- 
- 排查并修复 `/root/.hermes/hermes-workspace` 生产启动未加载 `.env` 的问题；恢复 `HERMES_API_TOKEN` 注入后，`/api/models` 从 401 恢复为 200，基础聊天可用。
- 将 `/root/.hermes/hermes-agent` 升级到 `v0.8.0` 并重建其 `venv`；确认基础接口 `/health`、`/v1/models`、`/api/jobs` 正常，但 `sessions/memory/skills/config` 仍缺失，判定为 workspace 与 agent 的增强 API 口径不一致，不再归因于前端。
- 配置 Telegram Bot `@Trey_trent_bot`，写入 bot token 与 allowlist（用户 `1690419247`）；补齐 Telegram 相关依赖后排查连接问题。
- 确认该机器直连 Telegram 超时，需经本机代理 `127.0.0.1:7893`；随后清理重复 Hermes gateway 实例、端口冲突与脏状态，最终网关日志显示 `Connected to Telegram (polling mode)`、`✓ telegram connected`，API 监听 `0.0.0.0:8642`。
- 发现 `hermes gateway status` 可能误报 not running；实际应以 PM2 在线、PID 文件、8642 监听和运行日志为准。

要点
- Hermes Workspace 基础聊天链路已恢复：`/api/models` 和 `/v1/chat/completions` 正常。
- 当前 Hermes Agent 版本虽已升级，但仍不提供 workspace 期望的 `sessions/memory/skills/config` 增强 API。
- Telegram 接入的核心前提：
  - bot token 已配置
  - `TELEGRAM_ALLOWED_USERS=1690419247`
  - 启动 Hermes gateway 时必须带代理环境，否则易超时
- 排障时已确认问题主要来自：环境变量未注入、Telegram 直连超时、重复 gateway 进程/端口占用/假状态。

产物
- `/root/.hermes/hermes-agent/venv`：重建并安装新版本 hermes-agent 与 Telegram 相关依赖。
- `/root/.hermes/.env`：已补 Telegram bot 配置与 allowlist（含代理相关运行需求）。

备注
- 后续若 Telegram 再掉线，优先检查是否以带代理环境启动 Hermes gateway，而不是先怀疑 token。
- 后续若要恢复 skills/memory/model switch 等能力，重点应转向匹配正确的 Hermes Agent / Workspace 版本与 API 口径，而不是继续修前端。



