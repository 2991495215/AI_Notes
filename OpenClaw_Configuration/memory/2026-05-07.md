# 2026-05-07

## 腾讯云 Zin 宕机修复（01:34 CST）
- 用户反馈“辛把自己玩死了”，已直接 SSH 到腾讯云 `101.43.64.135` 排查。
- 结论：并非整机或代理故障；`mihomo` 与 Hermes 仍在运行，故障点是 OpenClaw Gateway 因配置非法反复启动失败。
- 根因：`/root/.openclaw/openclaw.json` 中 `agents.defaults.compaction.mode` 为当前版本不接受的值；当前版本只允许 `default` / `safeguard`。
- 处理：执行 `openclaw doctor --fix`，自动修复配置，并清理了 1 条失效的 `plugins.allow` 项（`a2a-gateway`）。随后重启 `openclaw-gateway.service`。
- 验证结果：
  - `openclaw gateway status` 显示 `Runtime: running`、`Connectivity probe: ok`
  - `systemctl --user status openclaw-gateway.service` 为 `active (running)`
  - `ss -lntp` 确认 `0.0.0.0:18789` 正在监听
- 附带观察：doctor 额外创建了缺失目录 `~/.openclaw/credentials`，并提示存在大量 orphan transcripts，但本轮未处理这些低优先级问题。
