# 2026-03-28

- 工作区新增插件目录：K:\openclaw-workspace\plugins
- 已将 K:\openclaw-workspace\Project\agency-agents 移动到 K:\openclaw-workspace\plugins\agency-agents
- 已将 K:\openclaw-workspace\Project\openclaw-a2a-gateway 移动到 K:\openclaw-workspace\plugins\openclaw-a2a-gateway
- 这是工作区路径结构调整，后续涉及这两个插件时优先使用 plugins 下的新路径。

- 已实际验证 Stephen → Zin 的 A2A 发送链路可用。
- 验证时间：2026-03-28 01:06 Asia/Shanghai
- 验证方式：使用 C:\Users\a2832\.openclaw\extensions\a2a-gateway\skill\scripts\a2a-send.mjs 向 http://100.86.234.21:18800 发送测试消息
- 使用 token：zin-a2a-20260327-token
- 对端返回：ACK。当前看到的发送来源是：当前 OpenClaw 会话里的用户消息，内容标记为 “Stephen 实测 A2A 联通性”。
- 结论：至少 Stephen → Zin 单向 A2A 消息链路当前已真实打通，不只是 Agent Card 可访问。

- 新增交接文档：K:\openclaw-workspace\notes\resources\a2a-tailscale-handoff.md
- 用于交接 Stephen ↔ Zin 的 A2A/Tailscale 排障结论、实测结果、根因判断与建议修复方向。

- 已修复 `C:\Users\a2832\.openclaw\extensions\a2a-gateway\skill\scripts\a2a-send.mjs` 的固定会话续传问题。
- 新增能力：按 `peer-url`（以及可选 `agent-id`）自动持久化并复用 A2A `contextId`，存储文件为 `C:\Users\a2832\.openclaw\a2a-contexts.json`。
- 新增参数：`--new-context`，可忽略/清除该 peer 已保存的上下文并开启新会话。
- 实测结果：连续两次向 Zin 发送消息时返回相同 `contextId=fce81db7-66c4-4d0e-8710-f64691fda522`，且第二条成功读到上一条上下文，回复 `SECOND_OK FIRST_OK`。
- 结论：通过修复发送侧 `contextId` 自动续传，A2A 固定会话链路已打通；后续若使用该脚本发送同一 peer，可稳定复用同一会话。

- 已将修复后的 `a2a-send.mjs` 从运行副本同步回源码目录：`K:\openclaw-workspace\Project\openclaw-a2a-gateway\skill\scripts\a2a-send.mjs`
- 已核对 SHA256，一致：`2325F45EA42A7ADDC2D2EFAAA4FC4641F9835F6E872F08B3BE78D64FB2D690E7`

- 新增 skill：`K:\openclaw-workspace\skills\a2a-agent-handoff\SKILL.md`
- 用途：统一多 AI 通过 A2A 协作时的接入规范、身份说明、固定会话约定、交接格式与定时经验同步。
- 已附带参考文件：
  - `K:\openclaw-workspace\skills\a2a-agent-handoff\references\message-templates.md`
  - `K:\openclaw-workspace\skills\a2a-agent-handoff\references\sync-protocol.md`
- 定时互相补充经验/技巧功能已整合进该 skill，不再单独拆新 skill。

- 已继续强化 A2A 协作体系：
  - `a2a-send.mjs` 新增 `--topic`，context 持久化键升级为 `peer + agentId + topic`
  - `a2a-agent-handoff` skill 已补充：哪些场景必须 `--new-context`、agent 命名白名单、每次经验同步 3–5 条重点限制、主从式同步（Stephen 主整理）

- 已为当前会话创建提醒型经验同步 cron：`A2A 经验同步提醒（每晚9点）`
- jobId：`306530a0-f0cb-40ff-8de8-9861bd3a2fd4`
- 调度：每天 21:00（Asia/Shanghai）
- 模式：Stephen 主整理；若存在需要同步/交接给其他 agent 的事项，则按 `a2a-agent-handoff` 规范处理；否则只整理本地可交接摘要，不强行发送。
- `a2a-agent-handoff` skill 已补充“共享建议 / 同步一下 / 同步进去任务 / 交接任务”触发规则，以及启用后建议提醒用户配置定时同步。

- 已借鉴 `self-improving` skill 的有效思路，并融合进当前本地体系，而不是原样安装。
- 已新增/强化规则：
  - 只从明确纠错、明确偏好、重复成功模式、重要任务后的简短复盘中学习
  - 不从沉默里瞎学
  - 同类规则短期重复 3 次以上，再考虑升为长期规则
  - 心跳只做轻量维护，不借机大规模自我反思或重写记忆
  - 重要任务后的简短复盘可写入 `memory/YYYY-MM-DD.md`

- 本轮已完成的关键事项汇总：
  1. 验证 Stephen → Zin 的 A2A 实际可发通，确认并修复了路径宣告不一致导致的不稳定问题，主链路统一为 Tailscale。
  2. 修复 `a2a-send.mjs`：支持自动持久化/复用 `contextId`，并新增 `--new-context`。
  3. 继续增强 `a2a-send.mjs`：新增 `--topic`，context 键升级为 `peer + agentId + topic`。
  4. 新建并完善通用 skill：`a2a-agent-handoff`，涵盖新 agent 接入、身份说明、固定会话、会话显示名、交接规则、经验同步、触发词、主从式同步等。
  5. 创建每晚 21:00 的提醒型经验同步 cron，Stephen 主整理。
  6. 借鉴 `self-improving` 思路并融合进现有体系：明确纠错才学习、不从沉默学习、重复 3 次再升长期规则、重要任务后才做简短复盘。
  7. 已将修复后的 `a2a-send.mjs` 同步回源码目录 `K:\openclaw-workspace\Project\openclaw-a2a-gateway\skill\scripts\a2a-send.mjs`。

## 104.168.24.38 codex-console 批量注册上限修复
- 确认 104.168.24.38 的 SSH 登录方式为本机现有密钥免密登录（`C:\Users\a2832\.ssh\id_rsa`），不是此前尝试的密码。
- 线上实际运行的是源码版 `codex-console`，路径：`/root/codex-console/codex-console`。
- 实际启动方式不是 systemd / pm2，而是手工后台进程：`/root/.local/bin/uv run webui.py`，监听 `127.0.0.1:8000`，前面由 OpenResty 反代。
- 已修改线上源码：
  - `/root/codex-console/codex-console/src/web/routes/registration.py`
  - `/root/codex-console/codex-console/templates/index.html`
- 已把批量注册上限从 `1000` 改为 `999999`，并重启 `webui.py`。
- 验证结果：前端页面显示 `注册数量 (1-999999)` / `max="999999"`；后端真实接口为 `POST /api/registration/batch`，提交 `count=1001` 已成功入队，不再被范围限制拦截。
- 注意：104 这台 SSH 偶发出现 `banner exchange: invalid format`，但密钥登录本身可用。

- 用户明确新的 A2A 协作偏好：Stephen ↔ Zin 允许并鼓励在固定会话里多轮往返交流；交流完成后应主动向用户发送总结，而不是等用户追问后再汇报。
- 用户还关心交流过程中的可见性：若当前渠道/工具支持，应尽量体现“正在处理/正在交流”这类状态；若不支持真实 typing，则至少在关键阶段主动给简短进度回执。
