# 2026-04-25

- 已本机实测验证 Trey（`http://100.86.234.21:18802`, agentId=`main`）A2A 返回链路：
  - 直接调用 `a2a-send.mjs` 不带 `--non-blocking --wait` 时，仅拿到首响 ACK：`status=received; goal_received=true; ack=trey-ok`
  - 带 `--non-blocking --wait` 时，可拿到最终完成内容
- 结论：当前问题不用修 Trey 本体，应修上游调用方式/展示方式；对 Trey 应按异步 task 模型处理，不能把 `message/send` 首响当最终答复。
- 已更新 `K:\openclaw-workspace\TOOLS.md`：补充本机 A2A 脚本路径、Trey peer 信息，并写死规则“对 Trey 发请求默认必须带 `--non-blocking --wait`；仅在明确做 ACK 测试时才允许不等待”。
- 已新增 `K:\openclaw-workspace\Scripts\trey-send.ps1`：Trey 专用 A2A wrapper，默认复用 `a2a-log-send.ps1` 并自动使用 `-NonBlocking -Wait`；可用 `-AckOnly` 切换为仅测收件。
- 额外确认：本机 A2A 脚本实际路径为 `C:\Users\a2832\.openclaw\extensions\a2a-gateway\skill\scripts\a2a-send.mjs`；此前 Linux 路径 `/root/...` 不适用于当前 Windows 环境。

