# 2026-03-27.md

## 执行流失误记录（06:02）
- 本轮暴露出一个明确执行问题：在用户已明确授权“继续/全权交给你”后，仍多次输出“我准备做什么 / 我正在做什么 / 还差一件事”之类的中间态播报，导致体验上表现为“说一下停一下”。
- 根因不是工具、权限或人格切换，而是执行纪律问题：
  1. 过早汇报计划，未等待任务闭环
  2. 多子任务并行时，在最后一个子项未收尾前就先回复阶段性状态
  3. 没有严格区分“最终结果”和“中间过程”
- 后续固定规则：
  - 用户已明确要求继续执行时，默认进入闭环执行模式
  - 能直接做完的任务，不再提前播报步骤
  - 只允许两种对外状态：
    1. 已完成（给结果）
    2. 真卡住（给卡点、原因、所需输入）
  - 不再输出“我现在去做 X / 我还差 1 步 / 如果你点头我继续”这类半截状态
- 该问题应视为长期有效行为修正，后续若再次出现，需要优先修复执行节奏，而不是继续解释。

## A2A / Tailscale 协作状态（15:35）
- Stephen ↔ Zin 当前稳定链路以 Tailscale 为准，双向 A2A 已打通。
- 当前记录的 Tailscale 地址：Stephen `100.80.230.42`；Zin `100.86.234.21`。
- 双方 A2A 对外入口均固定为 `http://<tailscale-ip>:18800`，其中 agent card 为 `/.well-known/agent.json`，JSON-RPC 为 `/a2a/jsonrpc`。
- 已验证 Stephen -> Zin 与 Zin -> Stephen 的 `message/send` 均成功；稳定可用方法名是 `message/send`，不是 `tasks/send`。
- 已完成真实业务验证：通过 A2A 驱动 Zin 在 `/root/openclaw-workspace/OKK.txt` 落地文件，内容为 `OKK`。
- 维护结论：优先用 Tailscale peer，不再把 FRP/Stephen 本机公网直连当作主方案；异常时先查 `tailscale status`、agent card、再测 `message/send`。

## A2A 调用策略修正（17:17）
- 当用户明确要求“让辛/Zin 回一句话”或类似跨机转达/交接动作时，必须优先直接使用已经打通的 A2A 链路处理。
- 不应先绕去 `sessions_list`、本地 session 编排或其他旁路，避免偏离已验证的主链路。
- 对 Stephen ↔ Zin 这组双机协作，默认主通道就是 Tailscale + A2A。

## A2A / Tailscale 巡检同步（17:35）
- 今日巡检未发现新的拓扑变更；当前稳定认知仍是：Stephen ↔ Zin 双向 A2A 以 Tailscale 为主通道。
- 当前稳定资料以 `K:\openclaw-workspace\notes\resources\a2a-tailscale-handoff.md` 为准；暂无需要新增的持久知识点。

## A2A / Tailscale 记录归一（17:44）
- 已复核并确认当前主链路记录口径一致：Stephen ↔ Zin 以 Tailscale IP + A2A 为唯一稳定主方案。
- 已补写 `K:\openclaw-workspace\notes\areas\dual-ai-collaboration.md`，把旧的 `Xin` 名称更正为 `Zin`，并补入当前主链路、入口地址、方法名与排障顺序。
- 本轮未改 OpenClaw 运行配置；原因是现有持久记录已明确：主链路已稳定、今日巡检无新拓扑变化，且当前工具环境下无法直接执行本机网络/服务实测。
- 当前继续采用的稳定口径：优先 Tailscale，优先 `message/send`，链路异常时先查 `tailscale status`、agent card、再测 A2A。

## A2A 同步边界新规则（20:50）
- 用户明确要求：Stephen 为本地 Win11 环境，Zin 为 Linux 云端环境；两侧环境差异必须严格隔离，避免把本地配置口径直接发给 Zin 造成记忆混淆。
- 今后 Stephen -> Zin 的默认同步内容应以“已完成任务、结果结论、踩坑经验、可复用做法”为主，而不是直接转发本地配置细节、路径实现或环境专属参数。
- 只有当任务本身明确要求 Zin 修改其云端配置，或需要对比两端配置差异时，才进入配置级同步；否则一律保持经验/结论级同步。
- 对外表达口径：优先说清“我们完成过什么、验证了什么、学到了什么”，不要把 Win11 本机细节混入 Linux 云端上下文。

## A2A / Tailscale 例行同步（23:34）
- 复核 `K:\openclaw-workspace\notes\resources\a2a-tailscale-handoff.md` 与当日记忆后，当前稳定状态无新增变更。
- 继续维持的主口径：Stephen ↔ Zin 双向协作以 Tailscale + A2A 为主链路；Stephen `100.80.230.42`，Zin `100.86.234.21`；方法名继续以 `message/send` 为准。
- 继续有效的业务级验证：已能通过 A2A 驱动 Zin 在 `/root/openclaw-workspace/OKK.txt` 落地 `OKK`，说明链路不仅可聊天，也可完成远端执行与结果回传。
- 本轮未发现需要新增到交接文档的稳定知识点，现有 `a2a-tailscale-handoff.md` 仍可作为当前 canonical handoff。
