- 2026-04-30：EvoMap 自动巡检仍无法继续，原因持续是本地凭据文件 `/root/openclaw-workspace/notes/integrations/evomap-node.md` 缺失；按“无实质进展静默”规则，仅在 cron 触发时简短回报“凭据未恢复/缺少可用凭据/本地凭据缺失”。
- 2026-04-30：用户把 HY2 两条线都切到域名：`us-hy2-01.thirty30.top`（104.168.24.38:20000）与 `us-hy2-02.thirty30.top`（23.19.228.7:20001）；用户确认两条线都只有在客户端开启 `allow insecure` 时才能正常使用。
- 2026-04-30：23.19.228.7 上用户自行运行 `V2bX` 安装脚本为 Node ID 2（Host-hy2）重新配置；排查结论是 `us-hy2-02.thirty30.top` 在 23 机 1Panel/OpenResty 中没有对应站点/`server_name` 配置，因此 `http` 模式 ACME 验证未被正确接入，`V2bX` 连续申请失败后触发 Let's Encrypt `429 rateLimited` / `too many failed authorizations`。
- 2026-04-30：为让 23 上 Host-hy2 先恢复可用，方案改为 `self` 自签证书路线：`V2bX` 使用 `CertMode=self`，域名仍为 `us-hy2-02.thirty30.top`，监听 `20001/udp`；结论是短期保持两条 HY2 都走“自签/非严格校验 + allow insecure 开”方案最稳。
- 2026-04-30：用户展示 1Panel 中 `us-hy2-01.thirty30.top` 存在“正常”的 Let’s Encrypt 证书记录；据此确认 104 上该域名并非没有正规证书，而是“HY2/V2bX 实际是否正在使用那张 1Panel 证书”仍待核实。后续若要正规化，应优先检查 104 上 `V2bX` 当前证书文件路径是否对齐 1Panel 证书文件。

