## 2026-03-29 MoeMail 部署与修复
- 按用户要求将 `beilunyang/moemail` 安装到 `/root/moemail`，使用 Cloudflare 与 GitHub OAuth 完成部署。
- Cloudflare 资源已创建并确认：
  - D1: `moemail-db`，ID `a517e5dd-32ef-435e-91e0-f99d4f7846ff`
  - KV: `moemail-kv`，ID `ad686ccbdec34965b80be92c79ca49a3`
  - R2: `moemail-r2`
  - Pages 项目: `moemail`
  - Pages 默认域名: `moemail-3ow.pages.dev`
  - Email Worker: `email-receiver-worker`
  - Cleanup Worker: `cleanup-worker`
- 域名与路由：
  - 用户正式入口使用 `https://mail.trent30.org`
  - `*.trent30.org -> moemail-3ow.pages.dev` 的泛解析 CNAME 已存在
  - 用户后续将 `mail.trent30.org` 绑定为 Pages 自定义域并已显示活动/SSL 启用
  - Email Routing 的 Catch-all 已由用户在 Cloudflare 控制台改为发往 Worker（按用户口述确认）
- 部署中遇到的重要问题与修复：
  1. 用户给的 Cloudflare token 一开始无效或仅只读，最终使用 Cloudflare Global API Key + 账号邮箱 `a2832893628@qq.com` 才完成写操作。
  2. Cloudflare 账号最初未启用 R2，启用后才继续部署。
  3. `moemail` 在 Cloudflare Pages 构建时报 `/favicon.ico` 未配置 Edge Runtime：
     - 已将 `/root/moemail/app/favicon.ico` 复制到 `/root/moemail/public/favicon.ico`
     - 删除 `app/favicon.ico` 避免其作为独立 route 导致 Pages 构建失败。
  4. GitHub 登录时报 500，经 Pages deployment tail 定位到：
     - `No such module "__next-on-pages-dist__/functions/async_hooks" imported from middleware.func.js`
     - 根因是 `/root/moemail/middleware.ts` 在 Cloudflare Pages middleware 中直接引入 `auth` / NextAuth 相关逻辑。
     - 已将 middleware 降级为仅处理语言跳转，API 请求直接放行，重新部署后问题解除。
- 关键当前状态：
  - 最新修复版 Pages deployment 成功地址：`https://acb250bf.moemail-3ow.pages.dev`
  - 正式建议登录地址：`https://mail.trent30.org/zh-CN/login`
  - GitHub 登录修复后让用户继续执行登录 + `https://mail.trent30.org/api/roles/init-emperor` 初始化皇帝角色。
- 本地项目改动是为适配 Cloudflare Pages 实战部署，不是纯上游原样部署；后续若重部署，需保留：
  - `public/favicon.ico` 替代 `app/favicon.ico`
  - 精简版 `middleware.ts`，避免在 middleware 中直接引入 NextAuth / async_hooks 链路。

## 2026-03-29 MoeMail Worker 化修复追加记录
- 由于 Cloudflare Pages + Next.js 15 + `@cloudflare/next-on-pages` 组合下，动态页面与 App Route 持续触发 `async_hooks` 相关 500，认证主线已明确改为：Pages 只保留前端，关键 auth/API 迁到独立 Worker。
- 独立 Worker 已建立并接管：`mail.trent30.org/api/* -> moemail-api-worker`。
- 已在 `/root/moemail/workers/api.ts` 实现并部署最小闭环接口：
  - `/api/auth/signin/github`
  - `/api/auth/github/callback`
  - `/api/auth/post-login`
  - `/api/auth/session`
  - `/api/auth/logout`
  - `/api/roles/init-emperor`
  - `/api/config`
- GitHub OAuth 主链路已打通到 callback：用户已能完成授权并看到 Worker 返回的登录成功页。
- callback 成功分支已改为“两跳模式”：
  1. callback 负责 `Set-Cookie + 302`
  2. 跳转到 `/api/auth/post-login` 再由页面 JS 自动请求 `/api/auth/session` 与 `/api/roles/init-emperor`
  这样避免“在设置 cookie 的同一响应里立刻 fetch，cookie 还没稳定带上”的问题。
- 已修复一个明确漏路由：`/api/auth/post-login` 最初未注册到 Worker 分发表，导致用户看到 `{"error":"Not Found"}`；后已补上并重新部署。
- 已定位 `init-emperor` 的一次真实 Worker 1101 根因为 drizzle relation 在 Worker 环境异常：
  - 报错：`TypeError: Cannot read properties of undefined (reading 'referencedTable')`
  - 处理：将 `handleInitEmperor` 改为纯 D1 SQL，绕过 drizzle relation/delete/insert。
- 当前最新收敛结论：
  - GitHub 授权成功 ✅
  - callback 成功 ✅
  - `post-login` 页面存在 ✅
  - `init-emperor` 的 drizzle 关联异常已绕开 ✅
  - 剩余主问题收缩为：**带登录 cookie 访问 `/api/auth/session` 时，Worker 仍会抛 1101，导致前端拿到 Cloudflare 错误 HTML 而不是 JSON。**
- 用户最新提供的关键现场证据：
  - 成功页显示：`session 返回的不是 JSON`。
  - 实际返回为 Cloudflare `Worker threw exception` 1101 页面。
  - 对应 Ray ID：`9e3dfe8e0ddfa709`。
- 额外排查经验：
  - `curl -I -L https://mail.trent30.org/api/auth/session` 常得到 404，这个结果不可靠，不能直接当服务真实状态；而实际 `GET` 行为与浏览器带 cookie 行为不同。
  - 要判断当前卡点，必须区分：未登录直连接口、浏览器带 cookie 访问、以及成功页内 JS fetch 这三种路径，不能混看。
- 下一次续接时，优先检查 `/root/moemail/workers/api.ts` 中 `/api/auth/session` 所调用的 session 读取逻辑；策略应是继续降级为最小实现：只验 cookie、查 user、角色查询走纯 SQL，先保证稳定返回 JSON。

- 后续已继续推进并完成关键收口：
  - `/root/moemail/workers/api.ts` 中 `/api/auth/session` 已改为纯 D1 SQL 读取，确认解决了“登录后 session 返回 HTML/1101 而不是 JSON”的问题。
  - 用户最新实测结果已确认闭环：`GitHub 登录成功` → `登录态已建立` → `初始化皇帝结果：{ "error": "已存在皇帝, 谋反将被处死" }`，说明 auth/session/init-emperor 主链路已打通，剩余只是业务规则（已有皇帝）而非登录故障。
  - Worker 侧又补齐了一批后台常用 API，并已正式部署在线：
    - `/api/config` GET/POST
    - `/api/config/email-service` GET/POST
    - `/api/webhook` GET/POST
    - `/api/webhook/test` POST
    - `/api/api-keys` GET/POST
    - `/api/api-keys/:id` PATCH/DELETE
    - `/api/roles/users` POST
    - `/api/roles/promote` POST
    - `/api/emails/send-permission` GET
    - `/api/auth/register` 当前仅返回 `501 Worker 侧暂未接管密码注册哈希逻辑`，作为占位，避免神秘 404。
  - 实测未登录访问这些新 Worker 接口时，已返回合理 JSON/权限状态，而不是 Worker 崩溃：
    - `/api/config` → 200
    - `/api/config/email-service` → 403 `权限不足`
    - `/api/webhook` → 401 `Unauthorized`
    - `/api/emails/send-permission` → 200 `{"canSend":false,"error":"未授权"}`
  - Pages 动态页面侧，为绕开 Cloudflare 上服务端 `auth()` / `checkPermission()` 的脆弱链路，已将这些页面改为客户端根据 `/api/auth/session` 决定显示/跳转：
    - `/root/moemail/app/[locale]/page.tsx`
    - `/root/moemail/app/[locale]/login/page.tsx`
    - `/root/moemail/app/[locale]/profile/page.tsx`
    - `/root/moemail/app/[locale]/moe/page.tsx`
    - 新增客户端壳文件：
      - `/root/moemail/app/[locale]/home-client.tsx`
      - `/root/moemail/app/[locale]/profile-client.tsx`
      - `/root/moemail/app/[locale]/moe-client.tsx`
  - 新版 Pages 已正式部署：`https://3f25384c.moemail-3ow.pages.dev`
  - 正式域名 `https://mail.trent30.org/zh-CN/login`、`/zh-CN/profile`、`/zh-CN/moe` 再次抓取时，已不再返回 Cloudflare 1101/500 报错页，而是正常的 Next 页面 HTML/RSC 响应，说明“页面直接炸”的核心问题已明显缓解。
  - 仍需保留的判断：HEARTBEAT 使用的 `curl -I -L` 对 `/api/auth/session` 常见 404 结果依旧不可靠，不能当真实业务状态；后续判断服务好坏要以真实 GET / 浏览器行为为准。

## 2026-03-29 MoeMail 创建邮箱域名收口追加记录
- 用户在 `https://mail.trent30.org/zh-CN/moe` 已可进入页面并触发创建邮箱，说明登录与主要页面链路已可用；新主问题收缩为“创建邮箱时报 `无效的域名`”。
- 已确认优先修前端创建弹窗，而不是继续怀疑 Worker 主逻辑：
  - `/root/moemail/app/components/emails/create-dialog.tsx` 中 `createEmail` 已增加 `fallbackDomain = config?.emailDomainsArray?.[0]?.trim() || ''`。
  - 实际提交域名改为 `(currentDomain || fallbackDomain).trim()`，并在空值时直接 toast `域名配置尚未加载完成`，阻止提交空 `domain`。
  - 提交体同步清洗：`name: emailName.trim()`，并在提交前 `setCurrentDomain(selectedDomain)` 保持前端状态一致。
- `/root/moemail/app/hooks/use-config.ts` 已补域名列表清洗：`split(',').map(trim).filter(Boolean)`，避免配置里的空格/空项污染前端 domain 选项。
- 为了让前端修复真正上线，处理了 `workers/api.ts` 新增代码引起的 TypeScript 构建阻塞：
  - `requireSessionUser` 已显式标注返回类型，避免被推断成 `Response | undefined` 影响 `npm run build:pages`。
  - 之后 `npm run build:pages` 成功通过，说明 Pages 构建阻塞点已清掉。
- 前端修复版 Pages 已重新正式部署：`https://5d8d5fe6.moemail-3ow.pages.dev`。
- 实测发现一个关键“分裂态”：
  - 最新 Pages 直连部署 `/api/config` 读到的是 `moemail.app`。
  - 正式域名 `https://mail.trent30.org/api/config` 读到的是 `trent30.org`。
  - 这说明当时线上处于 Pages 前端与 Worker/KV 域名配置未完全对齐的状态，不能只看一边下结论。
- 为了不再被前端旧缓存/旧 domain 值卡住，已在 `/root/moemail/workers/api.ts` 的邮箱创建逻辑加入后端兜底：
  - 若前端提交的 `domain` 无效，但允许域名列表里只有一个域名，则 Worker 自动回退到该唯一域名而不是直接报 `无效的域名`。
  - 该 Worker 容错版已正式部署，版本 ID：`363818cb-2f79-4cb2-9a31-4b1f3933b33c`。
- 用户后续实测结果非常关键：
  - `https://mail.trent30.org/zh-CN/moe` 已显示“创建成功”。
  - 但新创建邮箱实际变成了 `CNjDKEU9@moemail.app`，而不是用户目标域名 `trent30.org`。
  - 这证明“创建邮箱功能已通”，剩余问题不是创建失败，而是 Worker 实际读取到的域名配置仍残留旧值。
- 已直接用 Wrangler 写入 Worker 绑定的 KV，将 `SITE_CONFIG` 中的 `EMAIL_DOMAINS` 强制改为 `trent30.org`，随后验证 `https://mail.trent30.org/api/config` 返回：
  - `{"defaultRole":"civilian","emailDomains":"trent30.org","adminContact":"","maxEmails":"20"}`
- 当前应保留的长期判断：
  - `mail.trent30.org` 自定义域并非“还在验证中”，因为用户已能打开 `https://mail.trent30.org/zh-CN/moe` 并成功创建邮箱；问题本质是 Pages / Worker / KV 三层配置值曾短暂不一致。
  - `http://trent30.org/` 打不开，不代表 `mail.trent30.org` 子域未生效；根域与子域是两回事。
  - 下一次续接时，优先继续确认：新创建邮箱是否已稳定变为 `@trent30.org`，以及 `moemail.app` 残留来源是否已完全清除。
