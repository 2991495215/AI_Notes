# 2026-03-06 斯蒂芬记忆日志

## 🛠️ 系统配置与维护
- **模型使用策略**：再次确认优先使用 `gemini-3.1-pro-high`。Claude 仅限写代码连续报错时作为杀手锏使用（省钱！）。
- **CoPaw 修复**：解决了 CoPaw 启动时的 401 认证错误。提取了阿里云百炼 Key 填入 `providers.json`，并将其活动模型设为 `qwen3-max`。
- **C 盘瘦身**：删除了旧的 Blender 素材、腾讯文档安装包及冗余的编辑器扩展压缩包，成功释放约 1.5GB 空间。
- **Skills 批量安装**：开始从 `K:\Downloads` 批量安装 skill。
    - **clawdbot-filesystem (v1.0.2)**：已安装并启用。解压至 `C:\Users\Administrator\.openclaw\skills\clawdbot-filesystem`。Gateway 已根据 Patch 重启。

## 📁 K 盘规范化重构 (核心变动)
- **防乱码规范**：为了彻底解决 PowerShell 环境下的中文乱码路径问题，将 K 盘核心目录全面英文规范化：
    - `K:\Core_Assets\` (核心资产：API 密钥、灵魂蓝图)
    - `K:\OpenClaw_Backup\` (记忆备份压缩包)
    - `K:\Scripts\` (脚本库)
    - `K:\Media_Images\` (图片素材)
    - `K:\Media_Videos\` (视频素材)
    - `K:\Deployments\` (项目部署)
- **同步更新**：已同步修改 `MEMORY.md` 中的所有目录索引。

## 🛡️ 备份功能升级
- **脚本修复**：修复了 `K:\Scripts\openclaw_backup.ps1` 中的编码错误和语法截断问题。
- **定时任务自动化**：将备份 Cron 任务 (`4b73dd2a-0453-4343-a723-925a48a73b44`) 升级为 `isolated` 模式，确保子 Agent 能在后台稳定执行备份指令。
- **状态确认**：测试运行成功，已在 `K:\OpenClaw_Backup\` 生成有效备份包。

## 🕵️ 遗留问题
- **源码失踪案**：观察到 `D:\Project\CoPaw` 在会话开始前已被清空（仅剩日志）。初步怀疑与 ReMe 初始化逻辑或目录误操作有关，待大哥哥指示进一步搜索。
