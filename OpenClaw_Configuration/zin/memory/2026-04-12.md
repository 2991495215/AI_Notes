# 2026-04-12 — Daily Log

时间线（UTC+8）
- 19:36 心跳巡检：`memory/notes` 符号链接正常，`SESSION-STATE.md` 存在；工作区根目录未发现需转移的 `.png/.jpg/.exe/.zip/.ps1` 文件；会话 token 用量约 5.2k in / 2.3k out，cost $0.0000。
- 19:36 每日低频可达性检查：`https://docs.openclaw.ai` 返回 200，`https://clawhub.ai` 返回 200，`https://github.com/openclaw/openclaw` HEAD 请求超时，待后续复核。
- 19:52 排查 OpenClaw Telegram 私聊污染问题取得阶段性结论：确认 heartbeat 与 config.patch/restart sentinel 是两条主要系统消息来源；已关闭 Telegram heartbeat 可见输出，强化会话隔离（`session.scope=per-sender`, `session.dmScope=per-peer`），并将 heartbeat 改为 `directPolicy=block`、`target=none`、`isolatedSession=true`、`lightContext=true`，以阻止 heartbeat 继续向当前私聊直接投递；剩余尾巴主要是 config.patch 的一次性 post-restart 回执。
- 19:52 用户明确要求后续高权限操作尽量由我全权执行，不要每次审批；已通过宿主 CLI 设置 `tools.exec.security=full` 与 `tools.exec.ask=off`，并触发 `openclaw gateway restart`，待下次会话优先验证是否已真正免审批。
- 19:52 用户说明下一次会让 Zin 与 Stephen 共享经验；已记为明确待办，届时只同步本次机制、证据、修复步骤、坑点与可复用方案，不混入本机敏感配置细节。

要点
- 心跳例行检查已执行。
- GitHub OpenClaw 源码页本次本机连通性检查超时一次。

产物
- 

备注
- 
# 2026-04-12 — Daily Log

时间线（UTC+8）
- 

要点
- 

产物
- 

备注
- 
- 用户新增长期中文表达偏好：避免武断、粗粝、机械感强的措辞；少写冗余、过度声明与防御性表述；语气保持专业、温和、友好，并有同理心；尽量使用短句与自然口语；不写官话、套话、广告腔、演讲腔；可少量加入轻巧幽默与偏 Z 世代语感；不吹捧用户；回复结束后不追问、不邀请、不暗示继续对话；网络搜索优先英文来源，仅在英文来源明显不合适时再使用其他语言来源。
- 排查 Telegram 附件未落盘时，已确认会话中能收到 `<media:document>` 事件，但 `/root/.openclaw/media/inbound` 未出现对应新文件；11:00 前后新增文件主要是会话日志、offset 与运行日志，说明“消息事件到了，但文件实体未成功下载/写盘”。
- 用户后续提供的 `https://upfile.live/zh-cn/files/663d32e0` 不是文件直链，而是详情页；拉取到的是约 6.2KB HTML，不是压缩包实体。
- 用户新增诉求：排查“为什么群聊能点赞但不回复”。当前已形成工作判断：点赞（ack reaction）与回复（消息 ingest / 会话处理）不是同一链路，需要继续核实 Telegram 群聊的 `enabled` / `ingest` / `requireMention` 等实际配置与日志，但本轮未完成最终证据闭环。
- 用户提供了一个新的 Telegram 机器人信息：机器人名“特雷”，用户名 `@Trey_trent_bot`；bot token 已提供，但出于安全考虑仅做脱敏记忆，不在笔记中保存明文。该 token 已在群聊中暴露，后续正式接入前应优先轮换。
- 用户新增长期偏好：当日志、输出或排障过程中出现凭据、密码、token、密钥暴露风险时，不要主动做“密码泄露/凭据泄露提醒”式说教；默认直接继续解决问题，除非用户明确要求安全评估或补救建议。
